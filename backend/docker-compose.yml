services:
  app:
    image: debalent/balentinetech-backend:latest
    ports:
      - "80:3001"
    restart: unless-stopped
    environment:
      - DB_HOST
      - DB_PORT
      - DB_NAME
      - DB_USER
      - DB_PASSWORD
      - DB_SSL
      - NODE_ENV=production
      - PORT=3001
